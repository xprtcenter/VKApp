import React from "react";
//import { firestore } from "../../firebase/firebase.utils";

export const PayrollEmpFunc = (payroll) => {
	//const initialState = {empId:"",empName:""}

	//const [data, setData] = useState(initialState)

	console.log(payroll);
};
